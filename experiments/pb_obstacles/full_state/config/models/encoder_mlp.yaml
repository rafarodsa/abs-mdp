features:
  type: mlp
  input_dim: ${model.obs_dims}
  output_dim: ${eval:${model.latent_dim} * 2}
  hidden_dims:
  - 128
  activation: relu
dist:
  type: diag_gaussian
  output_dim: ${model.latent_dim}
  min_var: 1.0e-06
  max_var: 1000000.0