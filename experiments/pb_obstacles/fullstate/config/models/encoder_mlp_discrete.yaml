features:
  type: mlp
  input_dim: ${model.obs_dims}
  output_dim: 2 #${eval:${model.latent_dim}*${model.encoder.codebook.embedding_dim}}
  hidden_dims:
  - 64
  - 64
  activation: relu
codebook:
  type: quantizer_st
  factors: 1 #${model.latent_dim}
  codes: 500
  embedding_dim: 2