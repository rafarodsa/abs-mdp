features:
  type: mlp
  input_dim: ${model.obs_dims}
  output_dim: 64
  hidden_dims:
  - 128
  - 64
  activation: relu
dist:
  type: diag_gaussian
  input_dim: ${model.encoder.features.output_dim}
  output_dim: ${model.latent_dim}
  min_std: 100
  max_std: 1000