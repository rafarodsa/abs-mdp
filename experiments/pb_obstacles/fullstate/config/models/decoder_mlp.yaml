features:
  type: mlp
  input_dim: ${model.latent_dim}
  output_dim: ${eval:${model.obs_dims}*2}
  hidden_dims:
  - 64
  activation: relu
dist:
  type: diag_gaussian
  output_dim: ${model.obs_dims}
  min_var: 1e-3
  max_var: 1e6