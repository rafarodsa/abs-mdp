features:
  type: mlp
  input_dim: ${model.latent_dim}
  output_dim: ${eval:${model.obs_dims}*2}
  hidden_dims:
  - 64
  activation: relu
dist:
  type: spherical_gaussian
  output_dim: ${model.obs_dims}
  input_dim: ${model.decoder.features.output_dim}
  min_std: 0.0001
  max_std: 10